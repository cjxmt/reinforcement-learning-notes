# 深度强化学习与控制中文学习笔记

## 目录

- [1. 深度强化学习基础](#1-深度强化学习基础)
- [2. 基于价值的方法](#2-基于价值的方法)
- [3. 基于策略的算法](#3-基于策略的算法)
- [4. 基于模型的控制](#4-基于模型的控制)
- [5. 重要公式与推导汇总](#5-重要公式与推导汇总)
- [6. 算法对比总结](#6-算法对比总结)
- [7. 易混淆概念辨析](#7-易混淆概念辨析)
- [8. 总结](#8-总结)

## 1. 深度强化学习基础

### 1.1 为什么需要深度强化学习

表格型方法需要为每个状态或状态-动作对保存一个值。当状态空间连续或维度很高时，表格会变得不可行。以 CartPole 举例：状态是 4 维连续向量，而动作空间是离散的；这类任务适合用函数近似表示 $Q(s,a)$。

深度强化学习的核心变化是：价值函数和策略由深度神经网络表示。深度强化学习利用深度神经网络进行价值函数和策略近似，使强化学习算法能够端到端解决复杂问题。

### 1.2 函数逼近

可以分为四类近似对象：

- 状态价值近似：$V_{\theta}(s)$。
- 动作价值近似：$Q_{\theta}(s,a)$。
- 随机策略近似：$\pi_{\theta}(a|s)$。
- 确定性策略近似：$a=\mu_{\theta}(s)$。

**数学含义**：参数 $\theta$ 不再只对应某个表格项，而是神经网络的权重。网络输入状态或状态-动作对，输出价值、动作概率或确定性动作。

**直观理解**：函数逼近让相似状态共享参数，从而能在高维连续空间中泛化；但它也带来训练不稳定、过拟合、数据需求大和计算需求高等问题。

### 1.3 深度强化学习中的不稳定性来源

在 DQN 中，直接用神经网络逼近 $Q_{\theta}(s,a)$ 会不稳定，原因包括：

- 连续采样得到的样本 $(s_t,a_t,s_{t+1},r_t)$ 不满足独立同分布。
- 输入样本来自连续交互轨迹，相关性强。
- $Q_{\theta}(s,a)$ 被频繁更新，导致训练目标也在变化。

深度强化学习因此引入经验回放和目标网络。

### 1.4 DQN 的输入、输出和训练目标

DQN 使用神经网络近似动作价值函数 $Q(s,a;\theta)$。对于离散动作空间，常见实现是输入状态 $s$，输出每个离散动作的 $Q$ 值。

- **输入**：状态 $s$。
- **输出**：各动作 $a$ 的估计价值 $Q(s,a;\theta)$。
- **训练目标**：让当前网络输出逼近 TD target。

Q-learning 的 target 值为 $y_t=r_t+\gamma\max_{a'}Q_{\theta}(s_{t+1},a')$，对应的优化目标通常是平方误差。

### 1.5 TD target、TD error 与 DQN loss

DQN 使用目标网络 $Q(s,a;\theta^-)$ 构造目标：

$$
y =
r + \gamma \max_{a'} Q(s',a';\theta^-)
$$

其中：

- $y$：TD target，即当前样本对应的监督学习目标。
- $r$：当前转移获得的奖励。
- $s'$：下一状态。
- $\theta$：当前评估网络参数。
- $\theta^-$：目标网络参数，来自较旧的网络，每隔 $C$ 步与评估网络同步。

DQN loss：

$$
L(\theta)
=
\mathbb{E}
\left[
\left(
y - Q(s,a;\theta)
\right)^2
\right]
$$

**推导过程**：

1. Q-learning 的一步目标为 $r+\gamma\max_{a'}Q(s',a')$。
2. 用神经网络表示动作价值：$Q(s,a;\theta)$。
3. 为避免目标随当前参数频繁变化，目标项中的下一状态价值由目标网络 $Q(s',a';\theta^-)$ 给出。
4. 将 TD target $y$ 看作监督学习标签，最小化当前预测 $Q(s,a;\theta)$ 与 $y$ 的平方误差。

**直观理解**：DQN 把 Q-learning 的表格更新改写成神经网络回归问题：网络输出越接近 TD target，动作价值估计越符合一步 Bellman 备份。

目标网络损失使用旧参数 $\theta^-$ 计算 target，目标网络每隔 $C$ 次迭代同步一次。

### 1.6 Experience replay 与 target network

**经验回放**：将每一步交互得到的 $e_t=(s_t,a_t,s_{t+1},r_t)$ 存入数据库 $D$，训练时从 $D$ 中均匀采样。

**为什么缓解样本相关性**：连续轨迹中的相邻样本高度相关；从 replay buffer 随机抽取小批量样本，会打散时间顺序，使训练数据更接近独立采样。

**目标网络**：使用较旧参数 $\theta^-$ 的网络 $Q_{\theta^-}(s,a)$ 计算 target，每隔 $C$ 步与评估网络同步一次。

**为什么需要 target network**：如果当前网络既预测 $Q(s,a;\theta)$ 又立即参与计算目标，训练标签会随每次参数更新快速漂移。目标网络把 target 固定一段时间，使优化问题更稳定。

### 1.7 DQN 算法步骤

DQN 流程可整理为：

1. 使用 $\epsilon$-greedy 策略与环境交互。
2. 将转移 $(s_t,a_t,s_{t+1},r_t)$ 放入 replay buffer。
3. 从 replay buffer 中采样 $k$ 个样本。
4. 用样本计算 DQN loss。
5. 更新当前 Q 网络参数 $\theta$。
6. 每隔 $C$ 次迭代同步目标网络参数 $\theta^-$。

## 2. 基于价值的方法

### 2.1 Value-based 方法的基本思想

基于价值的方法先学习价值函数，再由价值函数导出策略。Q-learning、DQN 及 DQN 改进算法属于 value-based 方法，其中 Q-learning 处理有限状态，DQN 可处理连续状态；这类方法学习值函数，学习过程中没有显式目标策略参数。

### 2.2 Monte Carlo 与 TD

MC 方法通过完整 episode 的 return 平均值估计价值。MC 直接从经验片段学习，不使用 bootstrapping，只能用于有限长度、有终止状态的 MDP。

TD 方法直接从经验片段学习，不需要预先获得状态转移或奖励模型，并通过 bootstrapping 从不完整片段学习。

TD(0) 更新为：

$$
V(S_t)
\leftarrow
V(S_t)
+
\alpha
\left[
R_{t+1}
+ \gamma V(S_{t+1})
- V(S_t)
\right]
$$

其中 TD target 是 $R_{t+1}+\gamma V(S_{t+1})$，TD error 是：

$$
\delta_t
=
R_{t+1}
+ \gamma V(S_{t+1})
- V(S_t)
$$

TD target 与 TD error 可以写为：

### 2.3 SARSA

SARSA 使用当前策略产生的 $(s,a,r,s',a')$ 元组更新动作价值：

$$
Q(s,a)
\leftarrow
Q(s,a)
+
\alpha
\left[
R
+ \gamma Q(s',a')
- Q(s,a)
\right]
$$

SARSA 是 on-policy TD control：两个动作 $A$ 都由当前策略选择，即行为采样和目标更新使用同一策略。

**直观理解**：SARSA 学的是“按当前探索策略实际会走的路线”的价值，因此 $\epsilon$-greedy 中的探索风险会反映到价值估计里。

### 2.4 Q-learning

Q-learning 是 off-policy 控制方法。行为策略 $\mu$ 用于采样，目标策略 $\pi$ 用于选择后续动作；目标策略是关于 $Q(s,a)$ 的贪心策略，行为策略是 $\epsilon$-greedy 策略。

Q-learning 更新：

$$
Q(S_t,A_t)
\leftarrow
Q(S_t,A_t)
+
\alpha
\left[
R_{t+1}
+
\gamma \max_{a} Q(S_{t+1},a)
-
Q(S_t,A_t)
\right]
$$

**变量含义**：

- 当前估计：$Q(S_t,A_t)$。
- TD target：$R_{t+1}+\gamma\max_{a}Q(S_{t+1},a)$。
- TD error：$R_{t+1}+\gamma\max_{a}Q(S_{t+1},a)-Q(S_t,A_t)$。
- $\alpha$：学习率。

**推导过程**：

1. 目标策略在下一状态选择贪心动作：

$$
\pi(S_{t+1})
=
\arg\max_{a} Q(S_{t+1},a)
$$

2. 因此下一状态动作价值目标为：

$$
R_{t+1}
+
\gamma Q
\left(
S_{t+1},
\arg\max_{a}Q(S_{t+1},a)
\right)
$$

3. 等价写成：

$$
R_{t+1}
+
\gamma \max_{a}Q(S_{t+1},a)
$$

4. 用“新估计 = 旧估计 + 学习率 $\times$ 误差”的增量形式更新 $Q(S_t,A_t)$。

**为什么是 off-policy**：采样动作来自行为策略 $\mu$，通常是 $\epsilon$-greedy；但 target 使用贪心动作的最大 $Q$ 值，对应目标策略 $\pi$。采样策略和学习的目标策略不同。

### 2.5 Greedy policy 与 epsilon-greedy policy

- **greedy policy**：总是选择当前 $Q$ 值最大的动作，偏向 exploitation。
- **$\epsilon$-greedy policy**：以较大概率选择贪心动作，以小概率随机探索，兼顾 exploration。

在 Q-learning 控制中，目标策略是关于 $Q$ 的贪心策略，行为策略是关于 $Q$ 的 $\epsilon$-贪心策略。

### 2.6 DQN、Double DQN 与 Dueling DQN

DQN 是把 Q-learning 的 $Q$ 函数估计改为神经网络估计的算法。Q-learning 中的 Q 函数估计可使用神经网络，得到 DQN 算法。

**DQN 核心**：

- 用 $Q(s,a;\theta)$ 近似动作价值。
- 用 experience replay 缓解样本相关性。
- 用 target network 稳定 TD target。

**Double DQN 与 Dueling DQN**：这两个名称在这部分内容中未作为独立内容展开。SAC 使用两个 $Q$ 网络，这一设计借鉴了 Double DQN 的思想，用较小的 $Q$ 值缓解过高估计问题。

Double DQN 的核心动机是减少 $\max$ 操作带来的过估计；Dueling DQN 通常把 $Q$ 分解为状态价值和动作优势两部分。但由于这里未展开具体网络结构和更新式，此处不作为主要算法展开。

## 3. 基于策略的算法

### 3.1 Policy-based 方法的基本思想

基于策略的方法直接显式学习目标策略。value-based 方法主要学习值函数并导出策略，而 policy-based 方法直接学习参数化目标策略。

设策略为 $\pi_{\theta}(a|s)$，其中 $\theta$ 是策略参数。策略可以由线性模型或神经网络建模，输入状态，输出动作概率分布。目标是最大化策略在环境中的期望回报。

### 3.2 轨迹、轨迹概率与目标函数

策略目标可写为：

$$
J(\theta)
=
\mathbb{E}_{\tau \sim \pi_{\theta}}
\left[
R(\tau)
\right]
$$

其中：

- $\tau=(s_0,a_0,r_1,s_1,a_1,\ldots)$：一条轨迹。
- $R(\tau)$：轨迹累计回报。
- $\pi_{\theta}$：生成轨迹中动作的参数化策略。

常用的目标函数形式是 $J(\theta)=\mathbb{E}_{s_0}[V^{\pi_{\theta}}(s_0)]$，也就是最大化初始状态分布下的策略价值。

**轨迹概率形式** 
在 MDP 中，轨迹概率可写为环境动力学和策略概率的乘积：

$$
P_{\theta}(\tau)
=
p(s_0)
\prod_{t}
\pi_{\theta}(a_t|s_t)
p(s_{t+1}|s_t,a_t)
$$

环境转移项不含 $\theta$，因此对策略参数求梯度时，关键项来自 $\pi_{\theta}(a_t|s_t)$。

### 3.3 Policy gradient 核心推导

从轨迹期望目标开始：

$$
J(\theta)
=
\sum_{\tau}
P_{\theta}(\tau)
R(\tau)
$$

对 $\theta$ 求梯度：

$$
\nabla_{\theta}J(\theta)
=
\sum_{\tau}
\nabla_{\theta}P_{\theta}(\tau)
R(\tau)
$$

使用 log-derivative trick：

$$
\nabla_{\theta}P_{\theta}(\tau)
=
P_{\theta}(\tau)
\nabla_{\theta}\log P_{\theta}(\tau)
$$

得到：

$$
\nabla_{\theta} J(\theta)
=
\mathbb{E}_{\tau \sim \pi_{\theta}}
\left[
R(\tau)
\nabla_{\theta}
\log P_{\theta}(\tau)
\right]
$$

由于环境转移概率不含 $\theta$，有：

$$
\nabla_{\theta}\log P_{\theta}(\tau)
=
\sum_t
\nabla_{\theta}
\log \pi_{\theta}(A_t|S_t)
$$

进一步可整理成：

$$
\nabla_{\theta} J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
G_t
\nabla_{\theta}
\log \pi_{\theta}(A_t|S_t)
\right]
$$

可得到策略梯度定理：

$$
\nabla_{\theta}J(\theta)
\propto
\mathbb{E}_{\pi_{\theta}}
\left[
Q^{\pi_{\theta}}(s,a)
\nabla_{\theta}\log\pi_{\theta}(a|s)
\right]
$$

并且 $\nabla_{\theta}\log\pi_{\theta}(a|s)=\nabla_{\theta}\pi_{\theta}(a|s)/\pi_{\theta}(a|s)$。

**为什么可以使用 $\nabla_{\theta}\log \pi_{\theta}(a|s)$**：它来自 log-derivative trick，可把“概率分布对参数的导数”转换成“从当前策略采样后的得分函数”，从而用采样估计梯度。

### 3.4 REINFORCE

REINFORCE 使用 Monte Carlo 方法估计 $Q^{\pi_{\theta}}(s,a)$。有限步环境策略梯度为：

$$
\nabla_{\theta}J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
\sum_{t=0}^{T}
\sum_{t'=t}^{T}
\gamma^{t'-t}
r_{t'}
\nabla_{\theta}
\log\pi_{\theta}(a_t|s_t)
\right]
$$

**算法步骤**：

1. 使用当前策略 $\pi_{\theta}$ 采样完整 episode。
2. 对每个时间步计算从该步开始的 return $G_t$。
3. 用 $G_t\nabla_{\theta}\log\pi_{\theta}(A_t|S_t)$ 估计梯度。
4. 用梯度上升更新策略参数 $\theta$。

**直观理解**：如果某个动作之后获得高 return，就提高该动作在该状态下的概率；如果 return 低，就降低其概率。

### 3.5 Baseline 与 advantage function

策略梯度可写成更一般的形式：

$$
g
=
\mathbb{E}
\left[
\sum_{t=0}^{T}
\psi_t
\nabla_{\theta}\log\pi_{\theta}(a_t|s_t)
\right]
$$

列出的 $\psi_t$ 可以是总回报、动作之后的回报、减去基准线的回报、动作价值函数、优势函数或 TD 残差。

优势函数定义为：

$$
A_{\pi}(s,a)
=
q_{\pi}(s,a)
-
v_{\pi}(s)
$$

A2C 的思想是通过减去基线函数标准化评论家的打分，以降低方差。优势函数定义为：

因此可写为：

$$
\nabla_{\theta} J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
A_{\pi}(S_t,A_t)
\nabla_{\theta}
\log \pi_{\theta}(A_t|S_t)
\right]
$$

**baseline 为什么不改变梯度期望**：

$$
\mathbb{E}_{a\sim\pi_{\theta}}
\left[
b(s)
\nabla_{\theta}\log\pi_{\theta}(a|s)
\right]
=
b(s)
\sum_a
\pi_{\theta}(a|s)
\nabla_{\theta}\log\pi_{\theta}(a|s)
$$

又因为：

$$
\pi_{\theta}(a|s)
\nabla_{\theta}\log\pi_{\theta}(a|s)
=
\nabla_{\theta}\pi_{\theta}(a|s)
$$

所以：

$$
b(s)
\sum_a
\nabla_{\theta}\pi_{\theta}(a|s)
=
b(s)
\nabla_{\theta}
\sum_a
\pi_{\theta}(a|s)
=
b(s)\nabla_{\theta}1
=
0
$$

**直观理解**：baseline 只改变每个动作得到的相对评分，不改变期望梯度方向；advantage 表示“该动作比当前状态平均水平好多少”，因此能降低方差并强化相对更优动作。

### 3.6 Actor-Critic

Actor-Critic 用一个可训练的值函数来指导策略学习。REINFORCE 用 MC 直接估计 $(s_t,a_t)$ 的值，而 Actor-Critic 建立可训练的值函数完成估计。

- **Actor**：策略 $\pi_{\theta}(a|s)$，负责选择动作。
- **Critic**：价值函数，例如 $Q_{\Phi}(s,a)$ 或 $V_{\omega}(s)$，负责评价 Actor 动作的价值。

Critic 可用 TD 残差训练。单样本价值函数损失：

$$
\mathcal{L}(\omega)
=
\frac{1}{2}
\left(
r
+ \gamma V_{\omega}(s_{t+1})
- V_{\omega}(s_t)
\right)^2
$$

对应梯度：

$$
\nabla_{\omega}\mathcal{L}(\omega)
=
-
\left(
r
+ \gamma V_{\omega}(s_{t+1})
- V_{\omega}(s_t)
\right)
\nabla_{\omega}V_{\omega}(s_t)
$$

**Actor-Critic 步骤**：

1. Actor 根据 $\pi_{\theta}(a|s)$ 采样动作。
2. 环境返回奖励和下一状态。
3. Critic 计算 TD error 或 advantage。
4. Actor 使用策略梯度更新 $\theta$。
5. Critic 使用 TD 损失更新 $\omega$。

### 3.7 On-policy、DDPG 与 SAC

**On-policy**：采样数据来自当前正在优化的策略。REINFORCE 和常见 A2C 属于 on-policy 风格。

进一步整理确定性策略梯度、DDPG 和 SAC：

- DDPG 使用确定性策略 $a=\mu_{\theta}(s)$，可理解为用 Actor 寻找使 Critic 的 $Q(s,a)$ 最大的动作；在此基础上，DDPG 使用经验重放、目标网络、批标准化和连续噪声。
- SAC 是最大熵强化学习下的 Soft Actor-Critic，学习随机策略，并在无模型算法中较高效；SAC 是离线策略算法，使用两个 $Q$ 网络缓解过高估计，并使用熵正则增强探索。

## 4. 基于模型的控制

### 4.1 Model-based RL 的基本思想

DQN、REINFORCE、Actor-Critic 都是无模型方法，即不建立环境模型；而基于模型的方法通常用神经网络学习环境模型，再利用模型帮助训练和决策。

环境模型通常包括：

- **transition model / dynamics model**：预测下一状态，例如 $\hat{P}(s_t,a_t)$ 或 $\hat{p}(s_{t+1}|s_t,a_t)$。
- **reward model**：预测奖励 $r(s_t,a_t)$。
- **learned model**：由真实环境数据训练得到的模型，可能是神经网络。

### 4.2 Planning 与 model predictive control

MPC 是一种迭代式、基于模型的控制方法。MPC 中不存在显式策略；每次采取动作时，先生成候选动作序列，再用当前状态和模型评估候选序列，最终执行最优序列的第一个动作。

MPC 目标是在第 $k$ 步最大化长度 $H$ 的累计奖励，并满足模型预测转移约束。

可整理为：

$$
a_{t:t+H-1}^{\ast}
=
\arg\max_{a_{t:t+H-1}}
\sum_{k=0}^{H-1}
\gamma^k
r(s_{t+k},a_{t+k})
$$

其中：

- $H$：预测时域，即向未来推演的长度。
- $a_{t:t+H-1}$：从当前时刻开始的一段候选动作序列。
- $r(s_{t+k},a_{t+k})$：模型预测轨迹上的奖励。

**为什么只执行第一个动作**：MPC 每次都根据当前真实状态重新规划。执行整个序列会放大模型误差；只执行第一个动作后，下一步用真实观测状态修正规划起点。

**为什么每一步重新规划**：环境可能随机，模型也可能有误差；重新规划可以利用最新真实状态，减少长期开环执行带来的偏差。

### 4.3 Shooting、CEM 与 PETS

**Random shooting**：随机生成 $N$ 条长度为 $H$ 的动作序列，每个动作从动作空间随机采样，再选择累计奖励最高的序列。

**Cross Entropy Method, CEM**：维护一个带参数的动作序列分布，根据采样结果更新分布参数，使高累计奖励动作序列的概率更高。相比 random shooting，CEM 能利用已有较好样本减少低质量动作采样，提高效率。

**PETS**：Probabilistic Ensembles with Trajectory Sampling，是使用 MPC 的基于模型强化学习算法。PETS 构建多个环境模型进行预测，并使用 CEM 进行模型预测控制。

PETS 同时考虑两类不确定性：

- **偶然不确定性**：系统自身随机性。
- **认知不确定性**：数据不足导致模型认知不足。

PETS 用高斯分布模型捕捉偶然不确定性：

$$
\hat{P}(s_t,a_t)
=
\mathcal{N}
\left(
\mu_{\theta}(s_t,a_t),
\Sigma_{\theta}(s_t,a_t)
\right)
$$

其模型损失包含加权平方误差和协方差正则项。第一项是马氏距离，第二项 $\log\det\Sigma_{\theta}$ 表示不确定性体积的对数，可防止方差坍缩。

### 4.4 Model error 与 compounding error

模型误差会影响控制效果，因为规划时使用的是学习到的模型而不是真实环境。推演越长，单步预测误差越可能累积。

在 MBPO 中，随着环境模型推演步数变长，模型累积的复合误差会快速增加，使模型结果不可靠；因此需要权衡推演步数带来的策略改进收益和模型误差负面影响。

MBPO 的做法是从真实环境样本作为分支起点，只用模型进行较短步数推演，而不是从初始状态开始完整推演。这可以限制模型累积误差，同时提升采样效率。

### 4.5 Model-based 与 model-free 的区别

| 维度             | Model-free                                    | Model-based              |
| ---------------- | --------------------------------------------- | ------------------------ |
| 是否学习环境模型 | 通常不学习                                    | 学习或使用环境模型       |
| 决策方式         | 直接学习价值函数或策略                        | 用模型预测未来并规划     |
| 代表方法         | Q-learning、DQN、REINFORCE、Actor-Critic、SAC | MPC、PETS、MBPO          |
| 优势             | 实现相对直接，不依赖模型准确性                | 样本效率较高，可利用规划 |
| 局限             | 样本需求大                                    | 受模型误差和复合误差影响 |

## 5. 重要公式与推导汇总

### 5.1 Return 递推公式

$$
G_t = R_{t+1} + \gamma G_{t+1}
$$

- **变量含义**：$G_t$ 是从 $t$ 开始的折扣回报；$R_{t+1}$ 是一步奖励；$\gamma$ 是折扣因子。
- **推导思路**：从 $G_t=R_{t+1}+\gamma R_{t+2}+\gamma^2R_{t+3}+\cdots$ 中提取第一项，剩余部分为 $\gamma G_{t+1}$。
- **直观解释**：当前累计回报由即时奖励和下一时刻累计回报组成。

### 5.2 Bellman expectation equation

$$
v_{\pi}(s)
=
\sum_a \pi(a|s)
\sum_{s',r}
p(s',r|s,a)
\left[
r + \gamma v_{\pi}(s')
\right]
$$

- **变量含义**：$v_{\pi}(s)$ 是策略 $\pi$ 下状态 $s$ 的价值；$p(s',r|s,a)$ 是环境模型；$r+\gamma v_{\pi}(s')$ 是一步奖励加后续价值。
- **推导思路**：把 return 递推代入状态价值定义，并对动作和环境转移求期望。
- **直观解释**：状态价值是所有可能动作和后续状态的加权平均。

### 5.3 Bellman optimality equation

$$
q_{\ast}(s,a)
=
\sum_{s',r}
p(s',r|s,a)
\left[
r + \gamma \max_{a'} q_{\ast}(s',a')
\right]
$$

- **变量含义**：$q_{\ast}(s,a)$ 是最优动作价值；$\max_{a'}q_{\ast}(s',a')$ 是下一状态最优动作价值。
- **推导思路**：把给定策略下的动作期望替换为最优动作选择。
- **直观解释**：当前动作价值等于当前奖励加上未来最优选择的折扣价值。

### 5.4 TD target 与 TD error

$$
\text{TD target}
=
R_{t+1}
+ \gamma V(S_{t+1})
$$

$$
\delta_t
=
R_{t+1}
+ \gamma V(S_{t+1})
- V(S_t)
$$

- **变量含义**：$\delta_t$ 是 TD error，表示目标与当前估计的差。
- **推导思路**：用 $R_{t+1}+\gamma V(S_{t+1})$ 近似 $G_t$。
- **直观解释**：TD error 衡量当前价值估计需要向一步 bootstrap 目标移动多少。

### 5.5 Q-learning update

$$
Q(S_t,A_t)
\leftarrow
Q(S_t,A_t)
+
\alpha
\left[
R_{t+1}
+
\gamma \max_{a} Q(S_{t+1},a)
-
Q(S_t,A_t)
\right]
$$

- **变量含义**：$\alpha$ 是学习率；中括号内是 TD error；$\max_{a}$ 对应贪心目标策略。
- **推导思路**：用 Bellman optimality target 替代表格 Q 的真实目标，再做增量更新。
- **直观解释**：把当前动作价值向“即时奖励 + 下一状态最优动作价值”移动。

### 5.6 DQN loss

$$
y =
r + \gamma \max_{a'} Q(s',a';\theta^-)
$$

$$
L(\theta)
=
\mathbb{E}
\left[
\left(
y - Q(s,a;\theta)
\right)^2
\right]
$$

- **变量含义**：$\theta$ 是评估网络参数；$\theta^-$ 是目标网络参数；$y$ 是 TD target。
- **推导思路**：把 Q-learning target 作为监督标签，用神经网络回归当前 $Q$。
- **直观解释**：DQN 用平方误差训练神经网络满足一步 Bellman 备份。

### 5.7 Policy gradient

$$
\nabla_{\theta} J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
G_t
\nabla_{\theta}
\log \pi_{\theta}(A_t|S_t)
\right]
$$

- **变量含义**：$J(\theta)$ 是策略目标；$G_t$ 是从当前步开始的 return；$\nabla_{\theta}\log\pi_{\theta}$ 是策略得分函数。
- **推导思路**：对轨迹期望求导，使用 log-derivative trick。
- **直观解释**：高回报动作的对数概率梯度会被正向放大。

### 5.8 REINFORCE

$$
\nabla_{\theta}J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
\sum_{t=0}^{T}
G_t
\nabla_{\theta}\log\pi_{\theta}(a_t|s_t)
\right]
$$

- **变量含义**：$G_t$ 用 MC 方式从完整 episode 估计。
- **推导思路**：用实际采样 return 替代未知 $Q^{\pi_{\theta}}(s,a)$。
- **直观解释**：REINFORCE 简单但方差较高，因为依赖完整轨迹回报。

### 5.9 Advantage function

$$
A_{\pi}(s,a)
=
q_{\pi}(s,a)
-
v_{\pi}(s)
$$

- **变量含义**：$q_{\pi}(s,a)$ 是动作价值；$v_{\pi}(s)$ 是该状态平均价值。
- **推导思路**：用动作价值减去状态基线，得到相对优势。
- **直观解释**：优势函数回答“这个动作比当前状态下的平均动作好多少”。

### 5.10 Actor-Critic update

Actor 可用：

$$
\nabla_{\theta} J(\theta)
=
\mathbb{E}_{\pi_{\theta}}
\left[
A_{\pi}(S_t,A_t)
\nabla_{\theta}
\log \pi_{\theta}(A_t|S_t)
\right]
$$

Critic 可用：

$$
\mathcal{L}(\omega)
=
\frac{1}{2}
\left(
r
+ \gamma V_{\omega}(s_{t+1})
- V_{\omega}(s_t)
\right)^2
$$

- **变量含义**：$\theta$ 是 Actor 参数；$\omega$ 是 Critic 参数。
- **推导思路**：Actor 用 Critic 给出的 advantage 或 TD residual 更新策略；Critic 用 TD target 学价值函数。
- **直观解释**：Actor 负责行动，Critic 负责评价行动质量。

### 5.11 Model predictive control objective

$$
a_{t:t+H-1}^{\ast}
=
\arg\max_{a_{t:t+H-1}}
\sum_{k=0}^{H-1}
\gamma^k
r(s_{t+k},a_{t+k})
$$

- **变量含义**：$H$ 是预测时域；$a_{t:t+H-1}$ 是候选动作序列。
- **推导思路**：用学习到的模型向前推演候选动作序列，选择累计奖励最大的序列。
- **直观解释**：MPC 每一步都重新规划，并只执行最优序列的第一个动作。

## 6. 算法对比总结

| 方法类型          |  是否学习价值函数 |       是否直接学习策略 | 是否学习环境模型 | on-policy/off-policy                         | 代表算法                          | 优点                                       | 局限                             |
| ----------------- | ----------------: | ---------------------: | ---------------: | -------------------------------------------- | --------------------------------- | ------------------------------------------ | -------------------------------- |
| value-based 方法  |                是 | 否，通常由价值导出策略 |               否 | Q-learning 是 off-policy；SARSA 是 on-policy | SARSA、Q-learning、DQN            | 思路清晰，适合离散动作；DQN 可处理高维状态 | 连续动作困难；函数逼近可能不稳定 |
| policy-based 方法 |            不一定 |                     是 |               否 | 多为 on-policy                               | REINFORCE、策略梯度               | 直接优化策略，可处理随机策略和连续动作     | 方差较高，样本效率可能较低       |
| actor-critic 方法 | 是，Critic 学价值 |       是，Actor 学策略 |           通常否 | 可 on-policy，也可 off-policy                | A2C、A3C、DDPG、SAC               | 结合价值评估和策略优化，降低纯策略梯度方差 | 结构更复杂，训练稳定性依赖设计   |
| model-free 方法   |    可学价值或策略 |       可直接或间接学习 |               否 | 取决于算法                                   | DQN、REINFORCE、Actor-Critic、SAC | 不依赖模型准确性，适用范围广               | 样本效率通常较低                 |
| model-based 方法  |              可选 |   可选，不一定显式策略 |               是 | 取决于与何种 RL 算法结合                     | MPC、PETS、MBPO                   | 可规划，样本效率高                         | 模型误差和复合误差会影响控制效果 |

## 7. 易混淆概念辨析

| 概念                                    | 区别                                                         |
| --------------------------------------- | ------------------------------------------------------------ |
| reward 与 return                        | reward 是一步即时反馈 $R_{t+1}$；return 是从某时刻开始的累计折扣奖励 $G_t$。 |
| value function 与 Q function            | $v_{\pi}(s)$ 评价状态；$q_{\pi}(s,a)$ 评价状态-动作对。      |
| policy evaluation 与 policy improvement | evaluation 估计给定策略的价值；improvement 根据价值改进策略，如 $\epsilon$-greedy 或 greedy。 |
| prediction 与 control                   | prediction 关注估计 $v_{\pi}$ 或 $q_{\pi}$；control 关注找到更优策略。 |
| model-free 与 model-based               | model-free 不建立环境模型；model-based 学习或使用模型进行预测和规划。 |
| value-based 与 policy-based             | value-based 学价值再导出策略；policy-based 直接学习参数化策略。 |
| on-policy 与 off-policy                 | on-policy 用当前策略采样并评估/改进同一策略；off-policy 用行为策略采样、学习另一个目标策略。 |
| TD target 与 TD error                   | TD target 是学习目标，如 $R_{t+1}+\gamma V(S_{t+1})$；TD error 是 target 与当前估计的差。 |
| actor 与 critic                         | Actor 学策略并选择动作；Critic 学价值函数并评价动作。        |
| experience replay 与 target network     | experience replay 随机重用历史样本，缓解样本相关性；target network 固定一段时间的目标计算，缓解目标漂移。 |

## 8. 总结

- 强化学习的核心是智能体在环境交互中学习策略，使长期 return 最大化。
- 无模型方法不依赖显式状态转移和奖励模型，而是直接从经验中学习价值或策略。
- MC 用完整 episode 的 return 估计价值，简单但只能等待终止且方差较高。
- TD 用 $R_{t+1}+\gamma V(S_{t+1})$ bootstrap 当前价值，能在线学习但引入偏差。
- SARSA 是 on-policy 控制，Q-learning 是 off-policy 控制，二者差异集中在下一动作如何用于 target。
- DQN 将 Q-learning 与神经网络结合，通过 experience replay 和 target network 缓解深度 RL 的训练不稳定。
- Policy gradient 直接优化参数化策略，使用 $\nabla_{\theta}\log\pi_{\theta}(a|s)$ 将采样轨迹转化为可估计梯度。
- REINFORCE 用 MC return 估计策略梯度，Actor-Critic 用 Critic 的价值估计降低方差。
- Advantage function 衡量动作相对状态平均水平的好坏，是 A2C/A3C 等方法的重要信号。
- DDPG 和 SAC 将 Actor-Critic 扩展到连续动作和离线策略场景，SAC 进一步引入最大熵目标提升探索。
- Model-based 方法学习环境模型并用 planning 或 MPC 决策，样本效率高但会受到模型误差累积影响。
- PETS 和 MBPO 代表了深度模型控制中的两条思路：前者用概率集成模型和 MPC，后者用短分支推演限制复合误差。
